public enum InstructionSet {
    // Register loading
    LOAD,

    // Logical
    AND,
    OR,
    XOR,

    // Arithmetic
    ADD,
    ADDCY,
    SUB,
    SUBCY,

    // Test and Compare
    TEST,
    TESTCY,
    COMPARE,
    COMPARECY,
}
